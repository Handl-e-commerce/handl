import { Box } from "@mui/material";

function Subscribe(): JSX.Element {
    console.log(window.location.pathname);
    
    return (
        <Box>This is the scaffholded subscribe path</Box>
    )
}

export {Subscribe};